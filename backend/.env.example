# Example env for local development (copy to backend/.env and fill values)
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=example_root_password
DB_NAME=Shay_Project
OPENAI_API_KEY=YOUR_OPENAI_KEY
PORT=3000
# JWT secret for signing tokens (override in production with a secure value)
JWT_SECRET=replace_this_with_secure_value
JWT_EXPIRES=7d
